# Runtime dependencies for Dino Hand Game
pygame>=2.5.0
opencv-python>=4.8.0
mediapipe>=0.10.0
numpy>=1.24.0

# Build-time (not shipped in the EXE, only needed on the developer machine)
pyinstaller>=6.0.0

